<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

  <nav class="bg-white shadow">
    <div class="container mx-auto px-6 py-3">
      <ul class="flex space-x-4">
        <li><a href="/" class="text-blue-500 hover:text-blue-600">Home</a></li>
        {{#if loggedIn}}
          <li><a href="/dashboard" class="text-blue-500 hover:text-blue-600">Dashboard</a></li>
          <li><a href="#" class="text-blue-500 hover:text-blue-600" id="logout">Logout</a></li>
        {{else}}
          <li><a href="/login" class="text-blue-500 hover:text-blue-600">Login</a></li>
        {{/if}}
      </ul>
    </div>
  </nav>

  <header class="bg-white shadow">
    <div class="container mx-auto px-6 py-3 flex justify-between items-center">
      <h1 class="text-xl font-semibold text-gray-700">
        <a href="/">Tech Blog</a>
      </h1>
    </div>
  </header>

  <main class="container mx-auto px-6 py-8">
    {{{body}}}
  </main>

  <footer class="bg-white shadow mt-8">
    <div class="container mx-auto px-6 py-4 text-center text-gray-600">
      &copy; Tech Blog
    </div>
  </footer>

<script src="/js/login.js"></script>
</body>
</html>